/**
 * Standard B3 options contract size (number of shares per contract)
 */
export const CONTRACT_SIZE = 100;

/**
 * Threshold (as a fraction of strike price) for At-The-Money classification.
 * E.g., 0.01 means within 1% of strike price.
 */
export const MONEYNESS_ATM_THRESHOLD = 0.01;
