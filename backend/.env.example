DATABASE_URL="postgresql://admin:password123@localhost:5432/advision?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-32-chars"
JWT_EXPIRES_IN="12h"
CORS_ORIGIN="http://localhost:5173"
COOKIE_SECURE="false"
COOKIE_DOMAIN=""
BRAPI_TOKEN=""
BRAPI_TIMEOUT_MS=10000
BRAPI_REQUEST_DELAY_MS=200
OPLAB_ACCESS_TOKEN=""

# URL pública do frontend (usada em links de e-mail, ex.: reset de senha).
# Se vazio, usa CORS_ORIGIN.
APP_URL="http://localhost:5173"

# SMTP para envio de e-mails (recuperação de senha).
# Sem estas variáveis, os e-mails são apenas logados no console (modo dev).
# Exemplo Brevo: SMTP_HOST=smtp-relay.brevo.com SMTP_PORT=587 SMTP_SECURE=false
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="Advision <no-reply@advision.app>"